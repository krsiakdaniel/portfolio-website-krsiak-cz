// Add any global test setup here
import '@testing-library/jest-dom'

// Example of extending expect
expect.extend({
  // Add custom matchers here if needed
})

// Clean up after each test
afterEach(() => {
  // Add any cleanup logic here if needed
})
