import { TEXT } from '@/localization/english'

const PartTimeLabel = () => {
  return (
    <small className="text-xs text-neutral-500">
      <em>{TEXT.partTimeJob}</em>
    </small>
  )
}

export default PartTimeLabel
