export const CAREER_PATH = {
  HIGHLIGHTED_STEPS_COUNT: 3,
} as const

// Resume preview image dimensions
export const RESUME_PREVIEW = {
  IMAGE: {
    WIDTH: 2152,
    HEIGHT: 3042,
  },
} as const

// Resume preview types
export const RESUME_TYPES = {
  PDF: 'PDF',
  TXT: 'TXT',
} as const
