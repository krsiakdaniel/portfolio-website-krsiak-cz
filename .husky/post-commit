echo "Running repo-wide quality gates…"

# 1. Type-check
bunx tsc --noEmit

# 2. Unit tests
bun test:jest --bail

# 3. E2E smoke (Chromium only, tag @smoke)
bun test:e2e:chrome --reporter=line --grep @smoke
